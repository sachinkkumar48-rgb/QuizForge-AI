name: titan_events
description: Event definitions, bus abstractions, and event handling for Project TITAN.
version: 0.1.0
publish_to: none

environment:
  sdk: "^3.3.0"

dependencies:
  titan_core:
    path: ../titan_core

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
