name: titan_network
description: Networking foundation, HTTP client abstraction, and interceptor framework for Project TITAN.
version: 0.1.0
publish_to: none

environment:
  sdk: "^3.3.0"

dependencies:
  http: ^1.2.0
  meta: ^1.11.0
  titan_core:
    path: ../titan_core

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
