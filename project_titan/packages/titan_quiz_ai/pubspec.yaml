name: titan_quiz_ai
description: AI Quiz Generation Pipeline package orchestrating PDF chunk extraction, prompt construction, LLM completion, JSON validation, and Quiz object parsing in Project TITAN.
version: 0.1.0
publish_to: none

environment:
  sdk: "^3.3.0"

dependencies:
  meta: ^1.11.0
  titan_core:
    path: ../titan_core
  titan_domain:
    path: ../titan_domain
  titan_ai:
    path: ../titan_ai
  titan_pdf:
    path: ../titan_pdf
  titan_quiz:
    path: ../titan_quiz

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
