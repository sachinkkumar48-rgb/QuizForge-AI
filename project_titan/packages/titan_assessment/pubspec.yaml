name: titan_assessment
description: Assessment engine, quiz evaluation, scoring, and test generation for Project TITAN.
version: 0.1.0
publish_to: none

environment:
  sdk: "^3.3.0"

dependencies:
  titan_core:
    path: ../titan_core
  titan_domain:
    path: ../titan_domain

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
