name: titan_storage
description: Storage abstraction, persistence layer, and repository implementations for Project TITAN.
version: 0.1.0
publish_to: none

environment:
  sdk: "^3.3.0"

dependencies:
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  meta: ^1.11.0
  titan_core:
    path: ../titan_core
  titan_domain:
    path: ../titan_domain

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
