name: titan_ai
description: AI services, LLM integration, prompt management, and AI engine for Project TITAN.
version: 0.1.0
publish_to: none

environment:
  sdk: "^3.3.0"

dependencies:
  meta: ^1.11.0
  titan_core:
    path: ../titan_core
  titan_network:
    path: ../titan_network
  titan_storage:
    path: ../titan_storage
  titan_security:
    path: ../titan_security

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
