name: garuda_pyq
description: GARUDA National PYQ Repository Foundation Package for Project TITAN. Permanent offline-first repository for Previous Year Questions of supported examinations.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  meta: ^1.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  test: ^1.24.0
