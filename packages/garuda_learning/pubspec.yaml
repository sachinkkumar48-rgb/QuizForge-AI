name: garuda_learning
description: Learning Objectives & Curriculum Framework package for Project TITAN. Deterministic, versioned, evidence-backed curriculum configuration and sequence layer.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  meta: ^1.11.0
  crypto: ^3.0.3
  garuda_case_law:
    path: ../garuda_case_law
  garuda_pyq:
    path: ../garuda_pyq

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  test: ^1.24.0
