name: garuda_evidence
description: GARUDA Evidence Engine foundation package for Project TITAN. Single gateway for all external current affairs and evidence ingestion.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  meta: ^1.11.0
  crypto: ^3.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  test: ^1.24.0
